# Copy this file to .env.local and fill in your Supabase project values.
# The anon key is meant to be public — Row-Level Security policies in the DB
# are what actually protect data. Do NOT commit a .env.local with real values.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
